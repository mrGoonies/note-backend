[project]
name = "note-backend"
version = "0.1.0"
description = "Develop API for frontend app writing in NextJS"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.117.1",
    "google-cloud-firestore>=2.21.0",
    "python-dotenv>=1.1.1",
    "uvicorn>=0.37.0",
]
